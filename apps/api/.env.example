# =============================================================================
# LAURx Portal API - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit the .env file to version control!

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=3001
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3002,http://127.0.0.1:3000,http://127.0.0.1:3002

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# Generate a secure random string (at least 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secure_random_string_at_least_32_characters_long

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# Choose your email provider: 'smtp', 'mailchimp', 'sendgrid', 'mailgun'
EMAIL_PROVIDER=smtp

# From email address (used in all outgoing emails)
FROM_EMAIL=noreply@mylaurelrose.com

# =============================================================================
# SMTP CONFIGURATION (for Gmail, Outlook, custom SMTP servers)
# =============================================================================
# For Gmail: smtp.gmail.com, port 587
# For Outlook: smtp-mail.outlook.com, port 587
# For Yahoo: smtp.mail.yahoo.com, port 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASS=your_gmail_app_password_16_characters

# =============================================================================
# MAILCHIMP TRANSACTIONAL (MANDRILL) CONFIGURATION
# =============================================================================
# Get your API key from: https://mandrillapp.com/settings/
# MAILCHIMP_API_KEY=your_mailchimp_transactional_api_key

# =============================================================================
# SENDGRID CONFIGURATION
# =============================================================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG.your_sendgrid_api_key

# =============================================================================
# MAILGUN CONFIGURATION
# =============================================================================
# Get your credentials from: https://app.mailgun.com/app/domains
# MAILGUN_API_KEY=your_mailgun_api_key
# MAILGUN_DOMAIN=your_mailgun_domain.com

# =============================================================================
# STRIPE CONFIGURATION
# =============================================================================
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe webhook endpoint secret (for webhook verification)
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# =============================================================================
# DEVELOPMENT/TESTING
# =============================================================================
# Set to true to enable detailed logging
DEBUG_EMAIL=false

# Set to true to prevent actual email sending (logs only)
EMAIL_DRY_RUN=false

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 
# 1. GMAIL SETUP:
#    - Enable 2-Factor Authentication on your Google account
#    - Go to https://myaccount.google.com/apppasswords
#    - Generate an App Password for "Mail"
#    - Use the 16-character password (remove spaces) as SMTP_PASS
#
# 2. MAILCHIMP SETUP:
#    - Sign up at https://mailchimp.com/
#    - Go to Transactional Email (Mandrill)
#    - Get your API key from Settings
#    - Set EMAIL_PROVIDER=mailchimp
#
# 3. SENDGRID SETUP:
#    - Sign up at https://sendgrid.com/
#    - Create an API key with Mail Send permissions
#    - Set EMAIL_PROVIDER=sendgrid
#
# 4. MAILGUN SETUP:
#    - Sign up at https://mailgun.com/
#    - Add and verify your domain
#    - Get API key and domain from dashboard
#    - Set EMAIL_PROVIDER=mailgun
#
# 5. TESTING:
#    - Start the API server: npm run dev
#    - Check console logs for configuration status
#    - Use the admin panel to send test emails
#
# =============================================================================
